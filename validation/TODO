example:
	属性绑定规则
	data-type绑定规则
	静态绑定
	各种配置
		懒验证模式
		忽略列表
		个性化错误元素
		自定义样式
		自定义显示容器
		信息包裹
	方法
	事件
	规则依赖
	真实示例：
		淘宝注册
TODO：
	依赖规则的实时校验 p3
	后台验证 p1
	<del>相等规则 p1 </del>
    <del>支持requried属性</del>
	pattern属性的支持 p2
	自定义属性的限制（保证并不是所有规则都能通过自定义属性添加）p2
	group支持 p1
	给表单元素动态添加删除验证规则的函数 p2
    日期格式校验支持参数，如：yyyy-MM-dd P2
    data-type属性支持参数

bugfix
    <del>focusout事件启动验证不通过自动获取焦点导致焦点锁死的问题 p0</del> //改成表单提交时的验证才会自动获取焦点