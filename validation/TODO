example:
	属性绑定规则
	data-type绑定规则
	静态绑定
	各种配置
		懒验证模式
		忽略列表
		个性化错误元素
		自定义样式
		自定义显示容器
		信息包裹
	方法
	事件
	规则依赖
	真实示例：
		淘宝注册
TODO：
	后台验证 p1
	group支持 p1
	<del>相等规则 p1 </del>
	<del>支持requried属性 p1</del>
	pattern属性的支持 p2
	<del>自定义属性的限制（保证并不是所有规则都能通过自定义属性添加）p2</del>
        <del>支持自定义的属性：</del>
            <del>min；max；range；minlength；maxlength；rangelength；required；pattern</del>
	<del>给表单元素动态添加删除验证规则的函数 p2</del> //通过原型方法addRule，可以将验证规则绑定到表单元素，不提供删除方法，感觉没有必要。
	<del>持参数，如：yyyy-MM-dd P2</del> //暂时取消对日期格式验证的支持
	<del>data-type属性支持xxx:xxx:xxx格式 p2</del>
	依赖规则的实时校验 p3

bugfix
	 <del>focusout事件启动验证不通过自动获取焦点导致焦点锁死的问题 p0</del>//改成表单提交时的验证才会自动获取焦点
	 chrome下format的问题
